/*
    Simply a custom class to handle rendering of selection states in the favourites view.
*/

#import <UIKit/UIKit.h>

@interface QFavouritesCustomCell : UICollectionViewCell
@property (strong, atomic) UIImageView* checkmarkView;
@property (strong, atomic) UIImageView* restaurantLogo;
@end
